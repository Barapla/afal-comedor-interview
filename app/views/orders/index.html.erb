<% content_for :title, "Mis pedidos" %>

<h1>Mis pedidos</h1>

<p>
  <% if DailyMenu.today %>
    <%= link_to "Hacer pedido del día", new_order_path, class: "button" %>
  <% else %>
    <span class="muted">No hay menú del día disponible.</span>
  <% end %>
</p>

<% own_orders   = @orders.reject(&:guest_order?) %>
<% guest_orders = @orders.select(&:guest_order?) %>

<% if own_orders.any? %>
  <table>
    <thead>
      <tr>
        <th>Fecha</th>
        <th>Estado</th>
        <th>Platillos</th>
        <th class="right">Total a descontar</th>
        <th></th>
      </tr>
    </thead>
    <tbody>
      <% own_orders.each do |order| %>
        <tr>
          <td><%= l(order.daily_menu.menu_date, format: :long) %></td>
          <td><%= order.status %></td>
          <td><%= order.order_items.map { |i| i.menu_item.dish.name }.to_sentence %></td>
          <td class="right">$<%= "%.2f" % (order.amount_due_cents / 100.0) %> MXN</td>
          <td><%= link_to "Ver", order %></td>
        </tr>
      <% end %>
    </tbody>
  </table>
<% else %>
  <p class="muted">Aún no tienes pedidos propios.</p>
<% end %>

<h2>Pedidos de mis invitados</h2>

<% if guest_orders.any? %>
  <table>
    <thead>
      <tr>
        <th>Invitado</th>
        <th>Fecha</th>
        <th>Estado</th>
        <th>Platillos</th>
        <th class="right">Cargo a nómina semanal</th>
        <th></th>
      </tr>
    </thead>
    <tbody>
      <% guest_orders.each do |order| %>
        <tr>
          <td><%= order.guest.name %></td>
          <td><%= l(order.daily_menu.menu_date, format: :long) %></td>
          <td><%= order.status %></td>
          <td><%= order.order_items.map { |i| i.menu_item.dish.name }.to_sentence %></td>
          <td class="right">$<%= "%.2f" % (order.items_total_cents / 100.0) %> MXN</td>
          <td><%= link_to "Ver", order %></td>
        </tr>
      <% end %>
    </tbody>
  </table>
<% else %>
  <p class="muted">Ninguno de tus invitados tiene pedidos.</p>
<% end %>
